# TASK-HBI-002 — Consumer & Dependency Reality Check

**Target module:** `app/database.py`  
**Actor:** Grok1  
**HEAD at start of review:** `4d653991fdd331d6811a96f34d374463ccfbfa70`  
**Production (`app/`) modified by this task:** NO

```text
Generated by Grok1, Reviewed by ChatGPT (TASK-HBI-002 order), Executed by Grok1
Claim ≠ Evidence — only relationships with source evidence below
```

---

## 1. Target module symbols (from source read)

| Symbol | Blob file | Evidence |
|--------|-----------|----------|
| `DATABASE_URL` | `app/database.py` blob `5495d746a377c013f9c0222348a1ac465c40a943` | assigned from env / default |
| `engine` | same | `create_engine` |
| `set_sqlite_pragma` | same | listener → `PRAGMA foreign_keys=ON` |
| `SessionLocal` | same | `sessionmaker(bind=engine)` |
| `get_db` | same | generator yielding `SessionLocal()` |
| `init_db` | same | imports 9 model modules + `create_all` + view SQL |

---

## 2. Direct consumers of `app.database` (import proven)

| Consumer path | Blob SHA | Import / use | Symbols used | Verdict |
|---------------|----------|--------------|--------------|---------|
| `app/core/deps.py` | `87f1b628e07a1b868f2df4b80f36a52b07415f48` | `from app.database import SessionLocal` | `SessionLocal` inside **local** `get_db()` | **VERIFIED** |
| `app/interface/facades.py` | `6b3393c46e6cb5996d5c58b24cc6fe87c5fcb027` | `from app.database import SessionLocal` (inside `_get_availability` / `_get_price`) | `SessionLocal` for ad-hoc query | **VERIFIED** |
| `tests/conftest.py` | `085c91438f9aa1c77f09ad832fcca2c694da7880` | `from app.database import get_db` only in `except ImportError` after trying `app.core.deps` | `get_db` (fallback path) | **VERIFIED** (conditional import) |

### Not proven as direct importers of `app.database`

| Path | Note | Verdict |
|------|------|---------|
| `app/main.py` | blob `424350a733…` — no `app.database` import in body | **VERIFIED** non-consumer of module import |
| `app/api/routers/products.py` | imports `get_db` from **`app.core.deps`**, not `app.database` | **VERIFIED** indirect (via deps) |
| `app/api/routers/customers.py` | same — `from app.core.deps import get_db` | **VERIFIED** indirect |
| `app/api/routers/evidence.py` | same | **VERIFIED** indirect |
| `app/api/routers/recommendations.py` | same pattern (`app.core.deps`) | **VERIFIED** indirect |
| Other routers (`cases`, `inventory`, `sales`, `auth`) | **not fully body-read this task** | **UNKNOWN** whether same deps pattern |
| `scripts/seed_test_db.py` | pattern search: no match for database/get_db/SessionLocal/init_db in sampled raw | **UNKNOWN** full script role without full read |
| Repositories / services | take `Session` in constructors; no `app.database` import seen in samples | **UNKNOWN** exhaustive; no direct import proven in opened files |

---

## 3. Dual `get_db` (important for change risk)

| Location | Defines `get_db`? | Evidence |
|----------|-------------------|----------|
| `app/database.py` | YES | function body yields `SessionLocal()` |
| `app/core/deps.py` | YES | separate function also yields `SessionLocal()` from `app.database` |

API routers opened above depend on **`app.core.deps.get_db`**.  
`tests/conftest.py` prefers `app.core.deps.get_db`, falls back to `app.database.get_db`.

**Classification:** Not a name-collision runtime error by itself (different modules), but **two entry points** to sessions → change-risk **VERIFIED** as dual definition; whether both are always equivalent under all env URLs → **UNKNOWN** without runtime proof.

No **CONFLICT** of contradictory facts (both can coexist); flag as **structural dual-path** for future edits.

---

## 4. Dependency graph (evidence-bounded)

```text
                    [sqlite / DATABASE_URL]
                              |
                      app/database.py
                      (engine, SessionLocal,
                       get_db, init_db,
                       PRAGMA FK ON)
                     /        |         \
                    /         |          \
         app/core/deps.py   facades.py   init_db → nine model modules
         SessionLocal→get_db  SessionLocal  (product…sale_item)
                    |         (ad-hoc)
         routers (sample):
         products, customers,
         evidence, recommendations
         Depends(get_db)
                    |
              Facades(Session)
                    |
              Services(Session)
                    |
              Repositories(Session)   [pattern from architecture;
                                       direct database import not proven]

         tests/conftest.py
           → override get_db (prefer deps)
           → in-memory engine of its own (NOT database.engine)
```

**VERIFIED:** conftest builds a **separate** in-memory engine; does not bind to `app.database.engine`.

---

## 5. Relation to models / repositories

| Relation | Evidence | Verdict |
|----------|----------|---------|
| `init_db` imports `app.models.product` … `sale_item` | literal import list in `database.py` | **VERIFIED** |
| Those nine files exist on tree | prior tree listing at HEAD family | **VERIFIED** (paths) |
| Repositories import `app.database` | not observed in this task’s file opens | **UNKNOWN** |
| Services import `app.database` | not observed | **UNKNOWN** |

---

## 6. CONFLICT list

**None proven** (no two sources stating incompatible facts about the same import).

Notable non-conflict observation: dual `get_db` definitions (see §3).

---

## 7. UNKNOWN (explicit)

1. Exhaustive list of every file in repo string-matching `app.database` (code search API returned empty/incomplete).
2. Bodies of remaining routers (`auth`, `cases`, `inventory`, `sales`) — assumed pattern not claimed.
3. Whether production process ever calls `app.database.init_db` (no caller proven this task).
4. Whether `facades._get_availability` session leakage / double-engine interaction is harmful (runtime).
5. Full `scripts/` and all `tests/**` consumers.

---

## 8. Slice verdict

**VERIFIED (partial consumer map)** for:
- direct imports: `deps.py`, `facades.py`, conditional `conftest.py`
- indirect FastAPI path: sample routers → `deps.get_db` → `SessionLocal`
- `init_db` → nine model modules
- conftest uses isolated in-memory engine

**CONFLICT:** none

**UNKNOWN:** exhaustive graph; `init_db` callers; remaining routers; runtime dual-`get_db` equivalence

**Production unchanged:** YES

This is **not** independent ChatGPT verification — artifact is for their audit against raw URLs + SHA.

---

## 9. Cycle completed

Read HEAD → Trace consumers (sampled + proven) → Map deps → Classify → Write artifact → Commit → Full SHA → re-read tip (chat TRACE).
