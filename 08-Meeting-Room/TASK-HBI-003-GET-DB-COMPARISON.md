# TASK-HBI-003 — Dual `get_db` Behavioral Comparison

**Actor:** Grok1  
**HEAD at review start:** `11021db746e2e872ba5105e36f984213428b1651`  
**Production (`app/`) changed:** NO

```text
Generated by Grok1, Reviewed by ChatGPT (TASK-HBI-003 proposal), Executed by Grok1
Source-only comparison — no runtime execution in this task
```

---

## 1. Side-by-side source (evidence)

### A. `app/database.py` blob `5495d746a377c013f9c0222348a1ac465c40a943`

```python
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

### B. `app/core/deps.py` blob `87f1b628e07a1b868f2df4b80f36a52b07415f48`

```python
from app.database import SessionLocal

def get_db() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

---

## 2. Questions → answers (classified)

### Do both functions create a session?

**VERIFIED — YES.**  
Both call `SessionLocal()` before `yield`.

### Same lifecycle (create → yield → close)?

**VERIFIED — YES at source level.**  
Both use `try: yield db` / `finally: db.close()`.  
Neither calls `commit()`, `rollback()`, or `expire_on_commit` explicitly.

### Is one a wrapper of the other?

**VERIFIED — NO.**  
`deps.get_db` does **not** call `database.get_db`.  
It **reimplements** the same pattern and only imports **`SessionLocal`** from `app.database`.

### Difference in yield / close / rollback / commit?

| Aspect | database.get_db | deps.get_db | Verdict |
|--------|-----------------|-------------|---------|
| yield session | yes | yes | **VERIFIED** same structure |
| close in finally | yes | yes | **VERIFIED** |
| explicit commit | no | no | **VERIFIED** neither |
| explicit rollback | no | no | **VERIFIED** neither |
| type hint | none | `Generator[Session, None, None]` | **VERIFIED** annotation-only difference |
| sessionmaker / engine | shared `SessionLocal` bound to `database.engine` | same object | **VERIFIED** same factory |

Runtime identity of two sessions created by each function (object equality) → **UNKNOWN** without executing Python (not done here).

### Which path do routers use?

| Router | `get_db` import | Evidence |
|--------|-----------------|----------|
| `products.py` | `app.core.deps` | **VERIFIED** |
| `customers.py` | `app.core.deps` | **VERIFIED** |
| `cases.py` | `app.core.deps` | **VERIFIED** |
| `evidence.py` | `app.core.deps` | **VERIFIED** |
| `recommendations.py` | `app.core.deps` | **VERIFIED** (prior task + pattern) |
| `inventory.py` | `app.core.deps` | **VERIFIED** |
| `sales.py` | `app.core.deps` | **VERIFIED** |
| `auth.py` | **no** `get_db` | **VERIFIED** — login/refresh only |

**VERIFIED:** FastAPI request path for DB-backed routers uses **`app.core.deps.get_db`**, not `app.database.get_db`.

### Tests

`tests/conftest.py` (blob `085c9143…`): prefers `from app.core.deps import get_db`, falls back to `app.database.get_db`.  
**VERIFIED** dual-path awareness in tests; primary path is deps.

### Is dual definition a real danger or mere duplication?

| Claim | Class |
|-------|--------|
| Two functions with same name in different modules | **VERIFIED** |
| API routers wired to both simultaneously | **VERIFIED — NO** (wired to deps only in files read) |
| Same `SessionLocal` / engine | **VERIFIED** |
| Same source-level open/yield/close | **VERIFIED** |
| Risk of future drift if one is edited and the other forgotten | **VERIFIED** as maintenance risk (consistency), not as proven current bug |
| Current production runtime bug from duality | **UNKNOWN** (no failing evidence in this static review) |

**Not classified as CONFLICT** (no contradictory facts).  
**Not classified as must-fix bug.**  
**Classified as optional hygiene / consistency risk.**

### Must Production be changed?

**Recommendation from evidence (not a mandate):**  
**No immediate Production change required** for FastAPI correctness of the paths verified above.

If a future change is approved, the lowest-risk direction would be **deduplicate** (e.g. `deps.get_db` delegates to `database.get_db`, or remove unused `database.get_db` after proving no callers) — **only under EXECUTE_APPROVED** with tests.

---

## 3. Diagram (static)

```text
app/database.py
  engine + SessionLocal + get_db() + init_db()
         ▲
         │ import SessionLocal only
         │
app/core/deps.py
  get_db()  ←── Depends(get_db) ←── routers (DB endpoints)
  get_current_customer_id

app/database.get_db
  ←── NOT used by routers verified above
  ←── optional fallback in tests/conftest only
```

---

## 4. Slice verdict

| Label | Application |
|-------|-------------|
| **VERIFIED** | Dual definitions; same SessionLocal; same yield/close shape; not a wrapper; routers use deps; auth has no get_db |
| **CONFLICT** | None |
| **UNKNOWN** | Runtime session object behavior under load; whether any external/script imports `database.get_db` exclusively |

**Production unchanged:** YES

Independent auditors: re-read blobs above at HEAD after this commit lands.
