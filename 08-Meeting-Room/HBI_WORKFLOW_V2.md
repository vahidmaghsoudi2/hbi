# HBI Workflow v2.0 — Engine + Guardrails (PILOT)

**Status:** ACTIVE PILOT — Evidence-bound
**Authority:** Product Owner — مهندس وحید مقصودی
**Process Owner / GitHub Minister:** Grok1
**Source of Truth:** `vahidmaghsoudi2/hbi`
**Registered:** 2026-08-23

```text
Generated by Grok1, Reviewed by Qwen1 (Reality Check FAILED on prior false SHA), Executed by Grok1
```

---

## 0. Admission of prior error

Prior chat claim of commit `a1b2c3d...` and files such as `GATE_6-1_RESOLUTION.md` / `HBI_AUTONOMOUS_WORKFLOW_V1.md` **had no physical existence** on GitHub at the time of the claim.

**Rule reaffirmed:** Claim ≠ Evidence. Only GitHub state + test/Actions result = Evidence.

---

## 1. Principle

| Layer | Role |
|-------|------|
| Workflow | Engine |
| Law | Guardrails |

Law must not replace the engine. Engine must not ignore guardrails.

---

## 2. Hard constraint

- Most AI members: READ only.
- Grok1 Write only when online and after verifiable commit (full 40-char SHA).
- Durable fallback: PO one-paste (`agent-jobs/po-one-paste.ps1`) without forged AI git identity and without token-in-URL.
- Test evidence preferred via **GitHub Actions** tied to SHA.

---

## 3. Engine (short)

```text
Qwen1 assigns TASK
→ Owner READs GitHub
→ Owner sends ONE package (TEMPLATES/MEMBER_PACKAGE.txt)
→ Write (Grok1 if online + verified push, else PO one-paste)
→ Actions on that SHA
→ Owner verifies DONE WHEN from GitHub
→ Short status to Qwen1 (TASK / full SHA / Run URL)
```

PO is not a message postman between AIs.

---

## 4. Guardrails

1. One Task = one Owner
2. Read before claim
3. Claim without full SHA or Actions URL = REJECTED
4. NO ASSUMPTION
5. Commit author = real executor; provenance only in commit **message**
6. No token in remote URL
7. Verify before Done
8. Prefer Change Package
9. Ceremony proportional to risk
10. Error OK; invisible error not OK
11. Rules must not freeze the project without cause
12. **Never invent SHA or file presence**

---

## 5. Pilot next step

Any member: submit MEMBER_PACKAGE for a small docs/scripts file → verified Write → Actions → Owner verifies.
