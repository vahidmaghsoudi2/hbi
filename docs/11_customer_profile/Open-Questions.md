# سؤالات باز — واحد پروفایل مشتری (برای تصمیم PO)

1. آیا `name` فعلی به `first_name` + `last_name` اختیاری شکسته شود یا در v1 همان `name` بماند؟
2. Guest بدون موبایل: شناسه پایدار چگونه ساخته شود؟ (CUST_GUEST_... یا session-bound)
3. آیا Case فعلی همان Visit است یا Visit موجودیت جدا روی Case؟
4. حداقل Consentهای اجباری در Pilot گالری کدام‌اند؟
5. POS: فقط fallback دستی در v1 کافی است؟
6. آیا فروشنده مجاز به ویرایش Fact تأییدشده است (با Audit) یا فقط مشتری؟
7. Retention: آرشیو بعد از چند ماه عدم مراجعه؟
8. زبان UI Intake: فقط فارسی؟

پس از پاسخ PO، Contract v1 به‌روز و قفل می‌شود.
