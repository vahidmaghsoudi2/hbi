*** WARNING: ACTIONS RUNNER DISPATCH WILL FOLLOW ***
This commit was made by an automated agent. It updates only tests/test_interface.py to set the sample_product fixture to ACTIVE via ProductRepository.update_governance_privileged().

After this commit I will dispatch the repository test workflow (if present) and run the targeted pytest commands. I will attach artifacts and the pytest output to the PR.
